#!/bin/bash
dir="$( cd "$(dirname "${BASH_SOURCE[0]}")" && pwd )"$"/"
cd "$dir"

# set -- "${@:1:2}" "new" "${@:4}"

if [ -z "$1" ]; then
  echo "No argument provided: please give tumblr shortname to generate"
  read varname
  set -- $varname
fi

python3 Tumblr.py $1
mv $1".txt" Chain/Cache/