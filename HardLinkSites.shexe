#!/bin/bash
dir="$( cd "$(dirname "${BASH_SOURCE[0]}")" && pwd )"$"/"
cd "$dir"

python3 -B -c "from done.File import smartBackup, getHome, join; smartBackup(join(getHome(),'Sites'),'Sites',hardlink=True)" > output.txt